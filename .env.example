# Copy to .env in the repo root. Used by Docker Compose only. Git-ignored; never commit it.
# Local development credentials. Must match DESKPILOT_DATABASE__PASSWORD in backend/.env.
# Generate a value with: openssl rand -hex 16

POSTGRES_PASSWORD=change-me