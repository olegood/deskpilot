"""Evaluating the agent: datasets, scoring, and a runner.

Deliberately small for now. Scoring here is deterministic - which tools were called,
what the ticket was labelled, whether a string does or does not appear in the answer.
Judging the prose belongs to the evals milestone, and needs a second model.
"""
